{
  "name": "comitia",
  "private": true,
  "scripts": {
    "test": "pnpm --filter @comitia/shared build && pnpm -r --filter './packages/*' test",
    "typecheck": "pnpm --filter @comitia/shared build && pnpm -r --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
