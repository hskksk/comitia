/** 実エンジンに渡すプロンプト（Claude Code / OpenCode 共通） */
export const ENGINE_PROMPT = `comitia ボードの MCP ツールが与えられている。get_briefing を呼び、その内容を踏まえて post で position を 1 件投稿し、最後に end_session を申し送りつきで呼べ。チャット出力での回答は不要。`;
